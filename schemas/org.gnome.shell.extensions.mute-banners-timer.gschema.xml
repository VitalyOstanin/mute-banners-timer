<?xml version="1.0" encoding="UTF-8"?>
<schemalist>
  <schema id="org.gnome.shell.extensions.mute-banners-timer"
          path="/org/gnome/shell/extensions/mute-banners-timer/">

    <!-- Last picked duration in minutes; expected to be one of the presets. -->
    <key name="last-duration" type="i">
      <default>1</default>
      <range min="1" max="60"/>
      <summary>Last chosen mute duration in minutes</summary>
      <description>
        The most recently selected mute duration, in minutes. Used as the
        pre-selected value in the on-banner dropdown and the panel menu.
      </description>
    </key>

  </schema>
</schemalist>
